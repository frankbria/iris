# Implementation Plans

Generated by the `improve` skill on 2026-06-21, against commit `65633a6`.
Each plan is self-contained: an executor with no prior context can run it from
the plan file plus the repo. Read the whole plan before starting, honor its
STOP conditions, and update your row when done.

Issue-title priority codes use `[PX.Y]`: **X** = priority tier (0 highest),
**Y** = order within the tier by importance/dependency.

## Execution order & status

| Plan | `[PX.Y]` | Title | Priority | Effort | Depends on | Issue | Status |
|------|----------|-------|----------|--------|------------|-------|--------|
| 001  | `[P0.1]` | Resolve runtime dependency vulnerabilities (simple-git, ws) | P0 | S | — | [#1](https://github.com/frankbria/iris/issues/1) | TODO |
| 002  | `[P1.1]` | Enforce concurrency cap (worker pool — p-limit@5 ESM breaks ts-jest); fix stale e2e tests | P1 | M | — | [#2](https://github.com/frankbria/iris/issues/2) | DONE |
| 003  | `[P1.2]` | Harden the `iris connect` WebSocket server | P1 | M | 001 (rec.) | [#3](https://github.com/frankbria/iris/issues/3) | DONE ([#12](https://github.com/frankbria/iris/pull/12)) |
| 004  | `[P1.3]` | Fix AccessibilityRunner URL handling; un-skip 21 tests | P1 | M | — | [#4](https://github.com/frankbria/iris/issues/4) | DONE ([#13](https://github.com/frankbria/iris/pull/13)) |
| 005  | `[P2.1]` | Add DX quality gates: typecheck/lint/format, .env.example, CI | P2 | M | — | [#5](https://github.com/frankbria/iris/issues/5) | DONE ([#14](https://github.com/frankbria/iris/pull/14)) |
| 006  | `[P2.2]` | Reconcile docs with reality; remove dead doc/test artifacts | P2 | S–M | 002, 004 (rec.) | [#6](https://github.com/frankbria/iris/issues/6) | DONE ([#17](https://github.com/frankbria/iris/pull/17)) |
| 007  | `[P2.3]` | Restrict config-file perms; close leaked DB/classifier handles | P2 | S | — | [#7](https://github.com/frankbria/iris/issues/7) | TODO |
| 008  | `[P3.1]` | Spike: surface cost/cache infra in the `visual-diff` CLI | P3 | M | 002, 007 (rec.) | [#8](https://github.com/frankbria/iris/issues/8) | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale).

## Recommended order

`001` → `002` → `004` → `003` → `005` → `007` → `006` → `008`.

Rationale: 001 is a one-command critical-vuln fix (do it first). 002 turns the
suite green (2 failing → 0). 004 recovers 21 skipped tests (real a11y coverage).
003 closes the network-exposure risk. 005 adds the gates that keep all of the
above verified in CI. 007 is small, independent hardening/leak fixes. 006 is best
done after 002+004 so the "tests pass" claims it writes are true. 008 is a
maintainer-facing spike, last.

## Dependency notes

- **003** recommends **001** first (the `ws` upgrade) but doesn't strictly require it.
- **006** should run AFTER **002** and **004** so the corrected pass/skip numbers
  it writes into README/CLAUDE.md are the real, improved ones.
- **008** touches the same runner path as **002** and the classifier-close added
  in **007**; landing those first avoids edit collisions.
- **005**'s CI will show red on `npm audit` until **001** lands and on `npm test`
  until **002** lands — that's expected and is itself the "done" signal.

## Findings considered and rejected (so they aren't re-audited)

- **Anthropic/Ollama vision "stubbed"** — false. `analyzeVisualDiff` is fully
  implemented for all three providers (`src/ai-client/vision.ts`); only the
  unused `translateInstruction` throws "not yet implemented". No action.
- **`page.evaluate(sequence.validator)` as RCE** (`a11y/keyboard-tester.ts:365`)
  — the validator comes from the developer's own `KeyboardTestConfig`, not remote
  input. Standard test-framework behavior, not a vulnerability.
- **Command / SQL injection** — none found. `translator.ts` uses `RegExp.exec`;
  `db.ts` uses static-string `db.exec` for schema. No shell exec from user input.
- **Path traversal in baseline/storage** — sanitization plus an `isValidImagePath`
  resolve-and-prefix guard already exist (`storage.ts`); LOW, not worth a plan now.
- **In-process LRU cache "race condition"** — single-threaded JS; only async
  interleaving, no data race of the claimed severity. LOW.
- **VisualReporter "god file" (979 lines)** split — works, 86% covered; deferred,
  not worth the refactor risk this cycle.
- **Phase 2C parallel execution unstarted** — that's roadmap (designed in
  `docs/phase2c_roadmap.md`), a maintainer prioritization call, not a defect.

## What was NOT audited

Standard-effort pass, hotspot-weighted. Not deeply covered: the benchmark
harness (`__tests__/benchmarks/`, excluded from Jest), the `examples/` apps, the
SQLite migration runner beyond a skim, and exhaustive coverage-percentage
verification per module (cited coverage gaps are agent-reported and should be
confirmed with `npx jest --coverage` before acting on plan 006's numbers).
